# Included by every location that sets its own headers: add_header directives
# are not inherited from the server level when a location defines any.
# (Proxied API responses already carry these headers from the backend, Helmet.)
add_header X-Content-Type-Options "nosniff" always;
add_header Strict-Transport-Security $hsts_header always;
